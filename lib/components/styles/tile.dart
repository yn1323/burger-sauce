import 'package:flutter/material.dart';

const expansionTileShape = Border(
  top: BorderSide(
    color: Colors.transparent,
    width: 1,
  ),
  bottom: BorderSide(
    color: Colors.transparent,
    width: 1,
  ),
);
